name: Run SA-MP Server on GitHub

on:
  push:
    branches: [ main ]

jobs:
  run-server:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ کلون پروژه
      uses: actions/checkout@v3

    - name: 🛠 نصب پیش‌نیازها
      run: sudo apt update && sudo apt install -y lib32z1 lib32gcc-s1

    - name: 🚀 اجرای سرور
      run: |
        chmod +x startup.sh
        ./startup.sh
